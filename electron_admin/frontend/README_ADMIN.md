# 后台管理系统前端

基于 Vue 3 + Element Plus 开发的现代化后台管理系统。

## 功能特性

### 🎨 精美UI设计
- 采用 Element Plus 组件库
- 渐变色登录页面
- 响应式布局设计
- 流畅的动画效果

### 📦 核心功能模块
- **用户管理**: 用户的增删改查、状态管理
- **角色管理**: 角色的创建、编辑、删除
- **权限管理**: 权限的配置和管理
- **菜单管理**: 动态菜单树形结构管理

### 🔐 权限控制
- JWT Token 认证
- 路由权限守卫
- 自动刷新令牌
- 登录状态持久化

### 💡 技术亮点
- Vue 3 Composition API
- Pinia 状态管理
- Axios 请求封装
- NProgress 进度条
- 响应拦截统一处理

## 技术栈

- **框架**: Vue 3.2.33
- **UI组件**: Element Plus 2.3.14
- **状态管理**: Pinia 2.1.6
- **路由**: Vue Router 4.0.14
- **HTTP客户端**: Axios 1.5.0
- **构建工具**: Vite 4.4.4
- **样式**: Less

## 快速开始

### 1. 安装依赖

```bash
cd electron_admin/frontend
cnpm install
# 或
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问: http://localhost:8080

### 3. 构建生产版本

```bash
npm run build
```

## 项目结构

```
src/
├── api/                  # API接口
│   ├── auth.js          # 认证接口
│   ├── user.js          # 用户接口
│   ├── role.js          # 角色接口
│   ├── permission.js    # 权限接口
│   └── menu.js          # 菜单接口
├── assets/              # 静态资源
├── components/          # 公共组件
├── layout/              # 布局组件
│   ├── Index.vue        # 主布局
│   └── components/      # 布局子组件
│       ├── Sidebar.vue  # 侧边栏
│       ├── Navbar.vue   # 顶部导航
│       └── AppMain.vue  # 主内容区
├── router/              # 路由配置
│   └── index.js         # 路由定义
├── stores/              # Pinia状态管理
│   ├── user.js          # 用户状态
│   └── app.js           # 应用状态
├── utils/               # 工具函数
│   └── request.js       # Axios封装
├── views/               # 页面组件
│   ├── Login.vue        # 登录页
│   ├── Dashboard.vue    # 首页
│   ├── Users.vue        # 用户管理
│   ├── Roles.vue        # 角色管理
│   ├── Permissions.vue  # 权限管理
│   └── Menus.vue        # 菜单管理
├── App.vue              # 根组件
└── main.js              # 入口文件
```

## 页面展示

### 登录页面
- 渐变背景设计
- 表单验证
- 密码显示/隐藏切换
- 响应式布局

### 首页仪表盘
- 统计卡片展示
- 系统信息展示
- 快捷操作入口
- 欢迎信息

### 管理页面
- 数据表格展示
- 分页功能
- 搜索筛选
- 新增/编辑/删除操作
- 表单验证

## API接口说明

### 基础配置
- 开发环境: `http://localhost:8000`
- 生产环境: 根据 `.env.production` 配置

### 认证接口
- `POST /auth/login` - 用户登录
- `POST /auth/refresh-token` - 刷新令牌
- `GET /users/me` - 获取当前用户信息

### 用户管理
- `GET /users` - 获取用户列表
- `POST /users` - 创建用户
- `GET /users/{id}` - 获取用户详情
- `PUT /users/{id}` - 更新用户
- `DELETE /users/{id}` - 删除用户

### 角色管理
- `GET /roles/` - 获取角色列表
- `POST /roles/` - 创建角色
- `GET /roles/{id}` - 获取角色详情
- `PUT /roles/{id}` - 更新角色
- `DELETE /roles/{id}` - 删除角色

### 权限管理
- `GET /permissions/` - 获取权限列表
- `POST /permissions/` - 创建权限
- `GET /permissions/{id}` - 获取权限详情
- `PUT /permissions/{id}` - 更新权限
- `DELETE /permissions/{id}` - 删除权限

### 菜单管理
- `GET /menus` - 获取菜单树
- `POST /menus` - 创建菜单
- `GET /menus/{id}` - 获取菜单详情
- `PUT /menus/{id}` - 更新菜单
- `DELETE /menus/{id}` - 删除菜单

## 默认账号

```
用户名: admin
密码: 123456
```

## 开发说明

### 添加新页面

1. 在 `src/views/` 创建页面组件
2. 在 `src/router/index.js` 添加路由
3. 在 `src/layout/components/Sidebar.vue` 添加菜单项

### 添加新接口

1. 在 `src/api/` 创建接口文件
2. 使用 `request` 工具发送请求
3. 在页面组件中调用接口

### 状态管理

使用 Pinia 进行状态管理:
- `useUserStore` - 用户信息和认证状态
- `useAppStore` - 应用配置和UI状态

## 注意事项

1. **后端服务**: 确保后端服务已启动 (`python manage.py start admin`)
2. **跨域配置**: 后端已配置CORS,允许所有来源
3. **Token管理**: Token存储在localStorage,自动添加到请求头
4. **错误处理**: 统一的响应拦截和错误提示
5. **路由守卫**: 未登录自动跳转到登录页

## 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge

## 许可证

MIT License
